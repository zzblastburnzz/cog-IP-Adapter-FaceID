build:
  gpu: true
  system_packages:
    - "libgl1"
    - "libglib2.0-0"
  python_version: "3.10"
  python_requirements: "requirements.txt"
  # Disable new runtime to avoid compatibility issues
  cog_runtime: false
predict: "predict.py:Predictor"