build:
  gpu: true
  system_packages:
    - "libgl1"
    - "libglib2.0-0"
  python_version: "3.10"
  python_requirements: "requirements.txt"
  # Disable new runtime completely
  cog_version: "0.8.0"
predict: "predict.py:Predictor"